platform :ios, '9.0'
use_frameworks!

target 'ForeignExchangeRates' do
    pod 'Alamofire', '~> 4.9.0'

    target 'ForeignExchangeRatesTests' do
    inherit! :search_paths
    # Pods for testing
    end

    target 'ForeignExchangeRatesUITests' do
    inherit! :search_paths
    # Pods for testing
    end

end
